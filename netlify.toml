[build]
  base = "Admin Panel"
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm run dev --prefix \"Admin Panel\""
  targetPort = 5173
  publish = "Admin Panel/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
