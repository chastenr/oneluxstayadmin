[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "functions"

[dev]
  command = "npx vite --host 0.0.0.0 --port 5175 --strictPort"
  port = 8889
  publish = "public"
  targetPort = 5175
