[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "functions"

[dev]
  command = "npx vite --host 0.0.0.0 --port 5173 --strictPort"
  targetPort = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
